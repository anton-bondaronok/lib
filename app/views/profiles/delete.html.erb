<div class="max-w-4xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
  <div class="bg-white rounded-3xl shadow-md overflow-hidden p-6 sm:p-8">
    <div class="flex space-x-6">
      <!-- Sidebar -->
      <%= render 'profiles/sidebar' %>

      <!-- Main Profile Content -->
      <div class="flex-1">
        <div class="max-w-xl mx-auto bg-white ">
          <h3 class="text-xl font-semibold text-gray-800 mb-4">Удалить мой профиль</h3>

          <div class=" text-gray-600">
            <%= button_to "Удалить мой профиль", profile_path, data: { confirm: "Вы уверены? Это действие необратимо.", turbo_confirm: "Вы уверены? Это действие необратимо." }, method: :delete, class: "text-red-600 hover:underline" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
